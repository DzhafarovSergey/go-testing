name: CI
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@4
      - uses: actions/setup-go@5
        with:
          go-version: '1.25.x'
          cache: true
      - name: Run tests
        run: go tests ./..
